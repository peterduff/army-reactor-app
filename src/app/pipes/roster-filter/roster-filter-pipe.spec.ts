import { RosterFilterPipe } from './roster-filter-pipe';

describe('RosterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RosterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

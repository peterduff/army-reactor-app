import { EnhancementFilterPipe } from './enhancement-filter-pipe';

describe('EnhancementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnhancementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

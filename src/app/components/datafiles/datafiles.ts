import { Component } from '@angular/core';
import {RouterLink} from "@angular/router";

@Component({
  selector: 'app-datafiles',
  imports: [RouterLink],
  templateUrl: './datafiles.html',
  styleUrl: './datafiles.scss',
})
export class Datafiles {
    constructor() {

    }

    addDatafiles(core: string, coreUrl: string) {

    }
}
